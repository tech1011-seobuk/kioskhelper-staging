# 프로젝트 작업 지침

- 한국어로 간결하게 설명하고 실제 코드와 Git 상태부터 확인한다.
- 이 저장소는 staging이다. 기본 변경 대상은 테스트 서버. 본 서버 적용은 사용자의 명시적인 요청이 있을 때만 진행한다.
- README.md, PROJECT_HANDOFF.md를 먼저 읽는다. 현재 코드/배포 결과와 문서가 다르면 확인 후 갱신한다.
- 3D는 photoism-model.js / photoism-picker.js / photoism-callouts.css를 수정하고 npm run build로 index.html에 통합한다. 내장된 minified Three 코드만 직접 수정하지 않는다.
- npm ci로 의존성을 설치하고 npm test로 검사한다. UI 변경은 브라우저에서 해당 화면과 모바일 크기를 확인한다. 실제 휴대폰을 검사하지 않았다면 그렇게 명시한다.
- 변경 후 소스, index.html, sw.js를 함께 커밋한다. 일반 UI 변경도 sw.js의 staging 캐시 버전을 갱신한다.
- 기존 배경과 브랜드 디자인을 유지한다. 사용자 요청 없이 화면 전체를 새 디자인으로 교체하지 않는다.
- 포토이즘 모델은 검정 본체/검정 모니터. 실제 없는 선반/돌출부를 추가하지 않는다. 자유 회전 대신 앞면/내부 버튼과 앞문 열림 후 부품 연결선/박스 방식 유지. 모바일 세로 스크롤을 가로채지 않는다.
- 테스트/운영 Supabase URL·공개키·캐시·오프라인 큐를 섞지 않는다. 비밀번호, 서비스 역할 키, 토큰, .env, 사용자 데이터 덤프를 저장소에 커밋하지 않는다.
- 브라우저의 기존 미저장 다이어그램을 임의로 저장하거나 초기화하지 않는다. DB SQL을 초기화 목적으로 재실행하지 않는다.
- 사용자 변경을 덮어쓰는 reset --hard, force push는 사용하지 않는다. 작업 종료 시 PROJECT_HANDOFF.md에 변경/검증/남은 작업을 기록한다.
